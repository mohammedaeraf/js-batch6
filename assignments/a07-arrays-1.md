# 📝 Assignment 06: Arrays

### 🎯 **Objective:**

Understand how to create, access, and manipulate arrays using built-in methods and loops.

---

## 🔹 Problem 1: Favorite Sports

**Task:**
Create an array called `sports` with at least 5 of your favorite movies.

- Print the **first** and **last** sports.
- Add a new sports to the end using `push()`.
- Remove the first sports using `shift()`.

---

## 🔹 Problem 2: Shopping List

**Task:**
Create an array `shoppingList` with some grocery items.

- Print the total number of items using `.length`.
- Check if "Milk" is present using `.includes()`.
- Add "Milk" at the beginning using `unshift()` if it's not already in the list.

---

### **Problem 3: Factorial of Numbers**  

#### Create an array containing 5 numbers. Use a loop to print factorial of all the 5 numbers. Use a function to calculate factorial.  

#### **Sample Input:**
```python
numbers = [3, 4, 5, 6, 7]
```

#### **Sample Output:**
```python
Factorial of 3 is 6
Factorial of 4 is 24
Factorial of 5 is 120
Factorial of 6 is 720
Factorial of 7 is 5040
```

---

### **Problem 4: Print Names in Reverse Order**

#### Create an array containing 5 names. Use a loop to print the names in reverse order.  

#### **Sample Input:**
```python
names = ["Alice", "Bob", "Charlie", "David", "Eve"]
```

#### **Sample Output:**
```python
Eve
David
Charlie
Bob
Alice
```